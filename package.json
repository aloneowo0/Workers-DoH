{
  "name": "cloudflare-doh-v2",
  "version": "2.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node scripts/build-config.cjs",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.5",
    "vitest": "~3.0.7",
    "wrangler": "^3.114.1"
  }
}
